import { StyleSheet } from "react-native";


const st = StyleSheet.create({
    textStyles: {
        color:'teal',
        fontSize: 20,
        fontWeight: 'bold',
        margin: 20,
    }
})

export default st;